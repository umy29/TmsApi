namespace TmsApi.Application.Hubs;

// Marker interface so Infrastructure can reference IHubContext<ITranscriptHub, ITmsHubClient>
// without a circular dependency on TmsApi.Api.
public interface ITranscriptHub
{
}
